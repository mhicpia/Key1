<!DOCTYPE html>
	<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
  	<title><%= content_for?(:title) ? yield(:title) : "Keys 1" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>

	</head>
	<body>

  <!-- Having a container row allows control of the size of the site -->
    <div id="divContainer" class="row"><div class="large-12 columns">
      <%= render "layouts/header" %> 

      <%= yield %>
      <%= javascript_include_tag "application" %>
    </div></div> <!-- end div id="divContainer" -->


	</body>
</html>
